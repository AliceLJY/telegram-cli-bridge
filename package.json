{
  "name": "telegram-cli-bridge",
  "version": "3.0.0",
  "description": "Telegram → Claude Code / Codex / Gemini CLI bridge via task-api (full CLI capabilities for all backends)",
  "main": "codex-bridge.js",
  "scripts": {
    "start:codex": "bun codex-bridge.js",
    "start:gemini": "bun gemini-bridge.js"
  },
  "dependencies": {
    "grammy": "^1.40.0",
    "https-proxy-agent": "^7.0.6"
  }
}
